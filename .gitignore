__pycache__/
*.py[cod]
.venv/
venv/
.env

# Built artifacts — regenerated by the Dockerfile at image build time, and
# locally by `python scripts/load_data.py && python scripts/train_*.py`.
# The raw CSVs are the source of truth and ARE committed (largest is 19MB,
# comfortably under GitHub's 50MB limit), so nothing here is unrecoverable.
data/recopulse.db
models/*.pkl
models/*.json

.DS_Store
.ipynb_checkpoints/
